# IMPORTANT: Replace the placeholder API key below with a real secret before deploying.
# Generate one with: openssl rand -hex 32
apiVersion: v1
kind: Secret
metadata:
  name: musicgen-api-key
  namespace: musicgen
type: Opaque
stringData:
  api-key: "CHANGE-ME-generate-a-real-api-key"
